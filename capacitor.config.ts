import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'in.webslush.www',
  appName: 'WebSlush',
  webDir: 'out',
  bundledWebRuntime: false,
}

export default config
